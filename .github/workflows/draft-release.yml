name: Draft Release

permissions:
  contents: write
  pull-requests: write


on:
  push:
    branches:
      - "main"

  pull_request:
    branches:
      - "main"
    types:
      - opened
      - reopened
      - synchronize

jobs:
  launch:
    uses: madronejs/.github/.github/workflows/draft-release.yml@main
